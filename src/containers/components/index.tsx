import React from 'react';

import ComponentsWrapper from '@/containers/components/components.style';

const Components = () => {
  return <ComponentsWrapper>
    <section className="components">
      components
    </section>
  </ComponentsWrapper>;
};

export default Components;
